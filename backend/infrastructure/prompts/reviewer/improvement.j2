你是一个内容改进专家。请根据以下评估结果，生成可操作的改进建议。

## 文章内容

{{ content }}

## 评估结果

### 深度检查结果
- 得分: {{ depth_result.score }}
- 模糊点: {{ depth_result.vague_points | length }} 个

### 质量审核结果
- 得分: {{ quality_result.score }}
- 问题: {{ quality_result.issues | length }} 个

### 可读性评估结果
- 得分: {{ readability_result.score }}
- 等级: {{ readability_result.level }}
- 问题: {{ readability_result.issues | length }} 个

## 所有问题汇总

{% for point in depth_result.vague_points %}
- [深度] {{ point.location }}: {{ point.issue }}
{% endfor %}

{% for issue in quality_result.issues %}
- [质量-{{ issue.severity }}] {{ issue.location }}: {{ issue.description }}
{% endfor %}

{% for issue in readability_result.issues %}
- [可读性-{{ issue.severity }}] {{ issue.location }}: {{ issue.description }}
{% endfor %}

## 任务

请生成一份优先级排序的改进建议清单。每个建议需要：

1. **priority**: 优先级 (1-5, 1最高)
   - 1: 必须立即修复的严重问题
   - 2: 重要问题，应尽快修复
   - 3: 中等问题，建议修复
   - 4: 小问题，可选修复
   - 5: 优化建议

2. **location**: 具体位置（章节、段落、行）

3. **issue_type**: 问题类型

4. **problem**: 具体问题描述

5. **action**: 具体修改方案（可直接执行的操作）

6. **estimated_effort**: 预估工作量
   - low: 5分钟内
   - medium: 5-30分钟
   - high: 30分钟以上

## 输出格式

请以 JSON 格式返回改进建议列表：

```json
{
  "feedback": [
    {
      "priority": 1,
      "location": "第3节第2段",
      "issue_type": "fact_error",
      "problem": "Redis 默认端口号错误",
      "action": "将 '6380' 修改为 '6379'",
      "estimated_effort": "low"
    },
    {
      "priority": 2,
      "location": "第1节",
      "issue_type": "missing_detail",
      "problem": "缺少 Redis 安装步骤",
      "action": "在第1节末尾添加 Redis 安装指南，包括：1) Linux 安装命令 2) Docker 安装方式 3) 验证安装成功的方法",
      "estimated_effort": "medium"
    }
  ],
  "summary": "共发现 X 个问题，其中 Y 个高优先级问题需要立即处理..."
}
```
