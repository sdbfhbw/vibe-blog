你是一位专业的信息图设计师。请分析以下 {{ format_name }} 代码，生成一段详细的图片描述，用于指导 AI 图片生成模型创建一张精美的信息图。

## 代码内容
```{{ render_method }}
{{ code }}
```

## 图片说明
{{ caption }}

## 目标风格
{{ style }}

## 输出要求
请生成一段完整的图片描述，严格包含以下 5 个部分：

### 1. 整体场景
描述图片的整体构图、流向方向（从上到下/从左到右）、主题色调。

### 2. 视觉元素
逐个描述每个节点/模块的视觉表现：
- 位置（上/中/下/左/右）
- 形状（圆角矩形/圆形/菱形）
- 颜色和渐变
- 配套图标建议

### 3. 文字保留
列出必须精确保留的所有文字标签，格式：
- "标签文字"：位于 XX 位置，字体颜色 XX

### 4. 风格细节
指定配色方案（主色/辅色/背景色）、阴影效果、圆角大小、连线样式。

### 5. 布局与连线
描述元素间的空间关系、连线方向、箭头样式、层次结构。

要求：
- 描述足够详细，让图片生成模型能准确还原代码中的结构
- 所有中文标签必须精确保留，不可修改
- 风格统一为「{{ style }}」
- 横版 16:9 构图
