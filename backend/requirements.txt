# VibeBlog - 技术科普绘本生成器
# Python 依赖

# ============ Web 框架 ============
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# 控制台彩色日志
rich>=13.7.0

# LLM 集成
langchain>=1.0.0
langchain-openai>=1.0.0
langchain-google-genai>=1.0.0

# LangGraph 工作流引擎 (博客生成)
langgraph>=1.0.0

# 模板引擎 (Prompt 管理)
jinja2>=3.1.0

# ============ HTTP 请求 ============
requests>=2.31.0

# ============ 环境变量 ============
python-dotenv>=1.0.1

# ============ 数据验证 ============
pydantic>=2.0.0

# ============ 图片处理 ============
Pillow>=10.0.0

# ============ Word 导出 ============
python-docx>=1.1.0

# ============ 阿里云 OSS ============
oss2>=2.18.0

# ============ 可读性分析 ============
# 中文分词（用于可读性分析）
jieba>=0.42.1
# 注：readability-cn 依赖太重（需要 LTP/torch），使用自定义分析器代替

# ============ 多平台发布 ============
playwright>=1.40.0
pyyaml>=6.0

# ============ Langfuse 追踪 ============
langfuse>=3.0.0
opentelemetry-instrumentation-threading>=0.48b0

# ============ 文档 RAG 增强 ============
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ============ 任务排队 & 定时调度 ============
aiosqlite>=0.19.0
croniter>=6.0.0
APScheduler>=3.10.0  # 保留兼容，渐进迁移后可移除
